# Core API
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0        # SQLite (dev local)
asyncpg==0.30.0          # PostgreSQL async (produção)
psycopg2-binary==2.9.10  # PostgreSQL sync (Alembic migrations)

# Pydantic — versões com wheels cp313 no PyPI
pydantic==2.10.0
pydantic-settings==2.6.1
python-dotenv==1.0.1

# HTTP e scraping
playwright==1.49.0
httpx==0.28.0
tenacity==9.0.0

# PDF e OCR
pdfplumber==0.11.4
pytesseract==0.3.13
Pillow>=11.0.0
pdf2image==1.17.0

# easyocr é opcional (pesado, ~1GB). Instale separadamente se necessário:
# pip install easyocr

# Agendamento
apscheduler>=3.10.4

# Testes
pytest>=9.0.0
pytest-asyncio>=1.0.0
